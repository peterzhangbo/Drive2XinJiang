# GitHub Pages 发布

## 结构

- `大兴安岭_2026中秋国庆/`：当前旅行源文件。
- `归档/新疆自驾_2026-08/`：新疆原文件，禁止为网站展示修改归档快照。
- `web/`：共享密码入口、右上角旅行切换和文档阅读器。
- `scripts/build-site.mjs`：读取两份行程并生成加密发布文件。
- `docs/`：GitHub Pages唯一发布目录，只包含入口、样式、脚本和加密数据。

## 生成与发布

修改大兴安岭逐日数据后，先从数据同步网页/简报；然后生成并验证加密发布包：

```sh
node scripts/render-daxing.mjs
node scripts/build-site.mjs
node scripts/validate-site.mjs
```

默认从归档原HTML读取旧密码设置；也支持环境变量 `TRIP_PASSWORD` 覆盖（不要提交明文密码）。生成后的 `docs/` 随代码提交，Pages从main分支的/docs目录发布；无需运行时服务器。

## 访问行为

- 站点首页解锁后默认打开大兴安岭。
- 右上角“大兴安岭 / XinJiang”在同一页面切换，保留各自阅读位置。
- 当前标签页使用sessionStorage保存派生密钥；刷新、浏览器前后退、切换无需重复输入。
- 关闭标签页后通常结束会话；浏览器会话恢复可能恢复sessionStorage。点击“锁定”可明确清除。
- 新标签页未共享会话时需再次输入；浏览器禁用存储时仍可在当前页面内切换。
- 旧“新疆主HTML”地址跳转到统一入口的新疆视图。
- 发布时移除旧HTML中的独立密码遮罩，归档原文件本身不变。
- Markdown文档随行程一起加密，在入口内打开，不发布明文副本。

## 范围

发布内容使用PBKDF2-SHA256（600000次）派生AES-256-GCM密钥，随机salt和IV。正确密码经解密验证，不依赖可伪造的“已登录”布尔值。每次重新构建更换salt，已有标签页下次刷新可能需重新解锁。

仓库目前公开，历史源文件及旧密码设置仍可从GitHub获得。网页加密不等于仓库私密化，也无法弥补原密码强度；本次按用户要求保留原密码，不修改仓库可见性。

## V0.5 内容补齐与发布前检查（2026-09-21）

- 对照新疆版补齐13天时间轴、逐日充电SOC情景与切换决策、午晚餐、景点玩法和准入、住宿候选、明确备线、补给应急、预算及本机确认清单。当前约4880–6050km，10晚酒店＋2晚条件车宿，含备用金预算11060–21520元；奇乾与临时绕行另计。
- 主页面直接展示证据表格、88处可拨号链接、47个地图搜索入口；导航入口只是搜点，不冒充已实测坐标/路线。保留原自然绿配色，按UI/UX检查提高正文尺寸、触控区域和表格可读性。
- Playwright本地加密发布包实测：原密码解锁、新疆来回切换、刷新保持会话、新文档弹窗；375/768/1024/1440视口无整页横向溢出；确认状态/备注/勾选本机保存后刷新正常，测试记录已清除。电量计算器对负余量和无效容量进行提示，打印前展开详情、打印后恢复，打印样式隐藏操作按钮。页面无运行错误；密码表单有浏览器通用可访问性建议，不影响解锁。
- 自动核对13天源数据、网页、简报、时间轴、地图关键词、锚点、预算求和、确认项数量与加密文档；新疆66个归档原文件不变，原密码可解密，篡改拒绝。
- 白鹿岛双犬住宿＋跨夜补能、长途充电主备空档、景区/商家直接接待确认仍未完成；这些是出行条件，不因页面功能通过而变成“已落实”。来源、电话、复核期限与失败退路已显示在页面。

## V0.4 发布前检查（2026-09-21）

- 逐日数据由`render-daxing.mjs`生成网页/简报；13天日期、路线、住宿、餐饮、充电和确认事项逐字段一致。
- `node scripts/validate-site.mjs`通过：66个新疆归档原文件未变，沿用原密码可解密双旅行，文档同步、篡改拒绝、旧链接跳转均通过。
- Playwright实测本机加密发布目录：原密码解锁、双旅行来回切换、刷新保持解锁、清单弹窗、D5锚点与详情展开通过；390px手机无横向溢出，桌面截图检查通过，页面无控制台错误。
- V0.4仍是条件方案：白鹿岛双犬住宿和跨夜补能未闭环，部分高速/普通路区间主备站仍缺，景区及商家未直接确认。发布不等于已完成实地落实。

## V0.3 历史发布检查（2026-09-20）

- `node scripts/validate-site.mjs`：13天计划在网页、文档与数据中一致；66个新疆归档文件校验未变；原密码可解密双旅行。
- 新疆历史校验：189项通过，0项失败。
- 浏览器实测：原密码进入、两份旅行切换、刷新保留会话、页内导航及文档弹窗正常；390px手机视口无横向溢出。
- 当前仍是路线计划版，具体充电站、服务区、住宿及两犬准入未全部核实，不以网站发布代替出行确认。
