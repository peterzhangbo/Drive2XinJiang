# GitHub Pages 发布

## 结构

- `大兴安岭_2026中秋国庆/`：当前旅行源文件。
- `归档/新疆自驾_2026-08/`：新疆原文件，禁止为网站展示修改归档快照。
- `web/`：共享密码入口、右上角旅行切换和文档阅读器。
- `scripts/build-site.mjs`：读取两份行程并生成加密发布文件。
- `docs/`：GitHub Pages唯一发布目录，只包含入口、样式、脚本和加密数据。

## 生成与发布

修改旅行源文件或web后执行：

```sh
node scripts/build-site.mjs
```

默认从归档原HTML读取旧密码设置；也支持环境变量 `TRIP_PASSWORD` 覆盖（不要提交明文密码）。生成后的 `docs/` 随代码提交，Pages从main分支的/docs目录发布；无需运行时服务器。

## 访问行为

- 站点首页解锁后默认打开大兴安岭。
- 右上角“大兴安岭 / XinJiang”在同一页面切换，保留各自阅读位置。
- 当前标签页使用sessionStorage保存派生密钥；刷新、浏览器前后退、切换无需重复输入。
- 关闭标签页后通常结束会话；浏览器会话恢复可能恢复sessionStorage。点击“锁定”可明确清除。
- 新标签页未共享会话时需再次输入；浏览器禁用存储时仍可在当前页面内切换。
- 旧“新疆主HTML”地址跳转到统一入口的新疆视图。
- 发布时移除旧HTML中的独立密码遮罩，归档原文件本身不变。
- Markdown文档随行程一起加密，在入口内打开，不发布明文副本。

## 范围

发布内容使用PBKDF2-SHA256（600000次）派生AES-256-GCM密钥，随机salt和IV。正确密码经解密验证，不依赖可伪造的“已登录”布尔值。每次重新构建更换salt，已有标签页下次刷新可能需重新解锁。

仓库目前公开，历史源文件及旧密码设置仍可从GitHub获得。网页加密不等于仓库私密化，也无法弥补原密码强度；本次按用户要求保留原密码，不修改仓库可见性。

## V0.3 发布前检查（2026-09-20）

- `node scripts/validate-site.mjs`：13天计划在网页、文档与数据中一致；66个新疆归档文件校验未变；原密码可解密双旅行。
- 新疆历史校验：189项通过，0项失败。
- 浏览器实测：原密码进入、两份旅行切换、刷新保留会话、页内导航及文档弹窗正常；390px手机视口无横向溢出。
- 当前仍是路线计划版，具体充电站、服务区、住宿及两犬准入未全部核实，不以网站发布代替出行确认。
